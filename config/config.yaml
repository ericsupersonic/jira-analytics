jira:
  base_url: "https://issues.apache.org/jira"
  auth_required: false

query:
  project_key: "KAFKA"
  jql: "project = KAFKA AND created >= -365d"
  max_results: 1000

output:
  output_dir: "output"
  top_users: 30

features:
  fetch_changelog: true
